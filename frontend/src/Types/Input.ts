export type InputType = {
  id: string;
  labelText: string;
  type: string;
  errors: any;
  register: any;
};
export type InputSelect = {
  label: string;
  data: { title: string; value: string }[];
  register: any;
  errors: any;
  id:string;
};
